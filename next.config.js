/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["localhost", "via.placeholder.com", "sergiomalagon.ociobinario.com"],
  },
};

module.exports = nextConfig;
